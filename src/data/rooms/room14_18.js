// Rooms 14-18: Advanced Pandas, SQL, Systems, Integration, Capstone

const room14 = { id: 14, lessons: [
  { title: 'Advanced Pandas', content: "## Advanced Pandas Techniques\n\n### Method Chaining\n\n```python\nimport pandas as pd\n\nresult = (df\n    .query(\"revenue > 1000\")\n    .assign(margin=lambda x: x[\"revenue\"] - x[\"cost\"])\n    .groupby(\"category\")\n    .agg(total_margin=(\"margin\", \"sum\"))\n    .sort_values(\"total_margin\", ascending=False)\n    .head(10)\n)\n```\n\n### MultiIndex\n\n```python\ndf.set_index([\"region\", \"product\"], inplace=True)\ndf.loc[(\"North\", \"Widget\")]  # Access specific combination\ndf.xs(\"North\", level=\"region\")  # Cross-section\n```\n\n### Apply & Map\n\n```python\n# Apply function to column\ndf[\"category\"] = df[\"revenue\"].apply(lambda x: \"High\" if x > 50000 else \"Low\")\n\n# Map values\ndf[\"status\"] = df[\"code\"].map({\"A\": \"Active\", \"I\": \"Inactive\"})\n```\n\n### Memory Optimization\n\n```python\ndf[\"category\"] = df[\"category\"].astype(\"category\")  # Saves memory\ndf[\"id\"] = df[\"id\"].astype(\"int32\")  # Downcast\n```" }
], exercises: [
  { id: 'ex1', title: 'Method Chaining Pipeline', description: 'Use method chaining to: filter sales > 100, add a "tier" column ("gold" if > 500 else "silver"), group by tier, and get the count and mean. Store in `result`.', starterCode: 'import pandas as pd\n\ndf = pd.DataFrame({\n    "customer": ["A","B","C","D","E","F","G","H"],\n    "sales": [50, 750, 200, 1200, 80, 600, 350, 900]\n})\n\nresult = (\n    df\n    # Your chain here\n)\n\nprint(result)', tests: [{ name: 'Result exists', code: 'assert result is not None', expectedOutput: null }] }
], scenarios: [], quiz: [
  { id: 'q1', type: 'multiple_choice', question: 'What does .assign() do in method chaining?', options: ['Assigns the DataFrame to a variable', 'Creates new columns', 'Assigns types', 'Assigns an index'], correct: 1 },
  { id: 'q2', type: 'multiple_choice', question: 'What is the benefit of using category dtype?', options: ['Faster string operations', 'Reduced memory usage for repeated values', 'Better sorting', 'Automatic encoding'], correct: 1 },
  { id: 'q3', type: 'multiple_choice', question: 'What does .pipe() do in Pandas?', options: ['Connects to a database', 'Passes the DataFrame to a custom function', 'Filters rows', 'Creates a pipeline object'], correct: 1 },
  { id: 'q4', type: 'multiple_choice', question: 'Difference between .apply() and .map()?', options: ['apply works on Series, map on DataFrames', 'map is for element-wise, apply is more flexible', 'No difference', 'map is faster for all cases'], correct: 1 },
  { id: 'q5', type: 'scenario', question: 'A DataFrame uses 4GB of RAM. How to reduce it?', options: ['Delete columns', 'Downcast numeric types and use category for strings', 'Use a list instead', 'Split into smaller files'], correct: 1 },
  { id: 'q6', type: 'multiple_choice', question: 'How to access data in a MultiIndex DataFrame?', options: ['df[key1][key2]', 'df.loc[(key1, key2)]', 'df.multi(key1, key2)', 'df.get(key1, key2)'], correct: 1 }
]};

const room15 = { id: 15, lessons: [
  { title: 'SQL with Python', content: "## SQL & Databases\n\n### sqlite3 (Built-in)\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect(\":memory:\")\ncursor = conn.cursor()\n\ncursor.execute(\"\"\"\n    CREATE TABLE employees (\n        id INTEGER PRIMARY KEY,\n        name TEXT NOT NULL,\n        dept TEXT,\n        salary REAL\n    )\n\"\"\")\n\ncursor.execute(\"INSERT INTO employees VALUES (1, 'Alice', 'Analytics', 85000)\")\nconn.commit()\n\ncursor.execute(\"SELECT * FROM employees WHERE salary > 70000\")\nrows = cursor.fetchall()\n```\n\n### Pandas + SQL\n\n```python\nimport pandas as pd\n\ndf = pd.read_sql_query(\"SELECT * FROM employees\", conn)\ndf.to_sql(\"new_table\", conn, if_exists=\"replace\", index=False)\n```\n\n### Parameterized Queries (Prevent SQL Injection!)\n\n```python\n# SAFE - parameterized\ncursor.execute(\"SELECT * FROM employees WHERE dept = ?\", (dept_name,))\n\n# UNSAFE - string formatting (NEVER DO THIS)\n# cursor.execute(f\"SELECT * FROM employees WHERE dept = '{dept_name}'\")\n```" }
], exercises: [
  { id: 'ex1', title: 'Build a Sales Database', description: 'Create an in-memory SQLite database with a "sales" table, insert data, and query total revenue by region using parameterized queries.', starterCode: 'import sqlite3\nimport pandas as pd\n\nconn = sqlite3.connect(":memory:")\ncursor = conn.cursor()\n\n# Create table\ncursor.execute("""\n    CREATE TABLE sales (\n        id INTEGER PRIMARY KEY,\n        region TEXT,\n        product TEXT,\n        amount REAL\n    )\n""")\n\n# Insert data\ndata = [(1,"North","Widget",450),(2,"South","Gadget",320),(3,"North","Widget",280),(4,"East","Gadget",510),(5,"South","Widget",190)]\ncursor.executemany("INSERT INTO sales VALUES (?,?,?,?)", data)\nconn.commit()\n\n# Query: Total by region using pandas\nresult = pd.read_sql_query("SELECT region, SUM(amount) as total FROM sales GROUP BY region ORDER BY total DESC", conn)\nprint(result)\n\nconn.close()', tests: [{ name: 'Query works', code: 'assert len(result) == 3', expectedOutput: null }] }
], scenarios: [], quiz: [
  { id: 'q1', type: 'multiple_choice', question: 'Why use parameterized queries (?) instead of f-strings in SQL?', options: ['Faster execution', 'Prevents SQL injection attacks', 'Better formatting', 'Required by Python'], correct: 1 },
  { id: 'q2', type: 'multiple_choice', question: 'What does sqlite3.connect(":memory:") do?', options: ['Connects to a remote server', 'Creates a temporary in-memory database', 'Reads from memory cache', 'Connects to RAM'], correct: 1 },
  { id: 'q3', type: 'multiple_choice', question: 'How to load a SQL query result into a Pandas DataFrame?', options: ['pd.from_sql()', 'pd.read_sql_query(query, conn)', 'pd.DataFrame(cursor)', 'pd.load_sql()'], correct: 1 },
  { id: 'q4', type: 'scenario', question: 'A user inputs their name into a search form connected to a database. How should you query?', options: ['f"SELECT * FROM users WHERE name = \'{input}\'"', 'cursor.execute("SELECT * FROM users WHERE name = ?", (input,))', 'Use eval() to build the query', 'Convert input to int first'], correct: 1 },
  { id: 'q5', type: 'multiple_choice', question: 'What does conn.commit() do?', options: ['Closes the connection', 'Saves changes to the database', 'Starts a transaction', 'Rolls back changes'], correct: 1 },
  { id: 'q6', type: 'multiple_choice', question: 'How to write a DataFrame to a SQL table?', options: ['df.to_sql("table", conn)', 'df.write_sql("table")', 'df.export("table", conn)', 'conn.insert(df)'], correct: 0 }
]};

const room16 = { id: 16, lessons: [
  { title: 'Systems Analytics', content: "## Systems Analytics & Automation\n\n### OS Module\n\n```python\nimport os\n\nos.listdir(\".\")           # List directory contents\nos.path.exists(\"file.csv\") # Check if file exists\nos.path.getsize(\"data.csv\") # File size in bytes\nos.makedirs(\"output/reports\", exist_ok=True)\n```\n\n### Regular Expressions\n\n```python\nimport re\n\nlog = \"2024-01-15 ERROR: Connection timeout at 10.0.0.1:8080\"\n\n# Extract IP\nip = re.search(r\"\\d+\\.\\d+\\.\\d+\\.\\d+\", log)\nprint(ip.group())  # 10.0.0.1\n\n# Extract date\ndate = re.search(r\"\\d{4}-\\d{2}-\\d{2}\", log)\nprint(date.group())  # 2024-01-15\n\n# Find all emails\nemails = re.findall(r\"[\\w.]+@[\\w.]+\", text)\n```\n\n### Logging\n\n```python\nimport logging\n\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\nlogger.info(\"Processing started\")\nlogger.warning(\"Missing values detected\")\nlogger.error(\"Failed to connect to database\")\n```" }
], exercises: [
  { id: 'ex1', title: 'Log Parser', description: 'Parse the log entries using regex. Extract the date, level, and message from each line. Store results as a list of dicts in `parsed_logs`.', starterCode: 'import re\n\nlogs = """2024-01-15 INFO: Server started on port 8080\n2024-01-15 WARNING: High memory usage detected (85%)\n2024-01-15 ERROR: Database connection failed\n2024-01-16 INFO: Backup completed successfully"""\n\nparsed_logs = []\n\nfor line in logs.strip().split("\\n"):\n    # Extract date, level, message using regex\n    pass\n\nfor log in parsed_logs:\n    print(log)', tests: [{ name: 'Parsed count', code: 'assert len(parsed_logs) == 4', expectedOutput: null }, { name: 'Has keys', code: 'assert "date" in parsed_logs[0] and "level" in parsed_logs[0]', expectedOutput: null }] }
], scenarios: [], quiz: [
  { id: 'q1', type: 'multiple_choice', question: 'What does re.findall() return?', options: ['First match only', 'A list of all matches', 'True/False', 'A match object'], correct: 1 },
  { id: 'q2', type: 'multiple_choice', question: 'What regex pattern matches one or more digits?', options: ['\\d', '\\d+', '\\d*', '[0-9]'], correct: 1 },
  { id: 'q3', type: 'multiple_choice', question: 'What logging level is most severe?', options: ['DEBUG', 'INFO', 'WARNING', 'CRITICAL'], correct: 3 },
  { id: 'q4', type: 'scenario', question: 'You need to monitor a directory for new CSV files and process them. Best approach?', options: ['Manual checking', 'While loop with os.listdir and sleep', 'Use watchdog library or scheduled task', 'Read all files at once'], correct: 2 },
  { id: 'q5', type: 'multiple_choice', question: 'What does os.makedirs("a/b/c", exist_ok=True) do?', options: ['Creates only directory c', 'Creates all directories in path, no error if exists', 'Raises error if exists', 'Creates only if empty'], correct: 1 },
  { id: 'q6', type: 'multiple_choice', question: 'How to run an external command from Python?', options: ['os.run()', 'subprocess.run()', 'exec()', 'system.call()'], correct: 1 }
]};

const room17 = { id: 17, lessons: [
  { title: 'Data Integration', content: "## API Consumption & ETL\n\n### Working with JSON APIs\n\n```python\nimport json\n\n# Simulated API response\napi_response = {\n    \"status\": \"success\",\n    \"data\": [\n        {\"id\": 1, \"name\": \"Product A\", \"sales\": 15000},\n        {\"id\": 2, \"name\": \"Product B\", \"sales\": 22000}\n    ]\n}\n\n# Process API data\nimport pandas as pd\ndf = pd.DataFrame(api_response[\"data\"])\n```\n\n### ETL Pipeline Pattern\n\n```python\ndef extract(source):\n    \"\"\"Read data from source.\"\"\"\n    return pd.read_csv(source)\n\ndef transform(df):\n    \"\"\"Clean and transform data.\"\"\"\n    df = df.dropna()\n    df[\"total\"] = df[\"price\"] * df[\"qty\"]\n    return df\n\ndef load(df, destination):\n    \"\"\"Save processed data.\"\"\"\n    df.to_csv(destination, index=False)\n\n# Execute pipeline\nraw = extract(\"sales.csv\")\nclean = transform(raw)\nload(clean, \"processed_sales.csv\")\n```" }
], exercises: [
  { id: 'ex1', title: 'Build an ETL Function', description: 'Create an ETL pipeline that takes raw data (list of dicts), cleans it (remove entries with missing "revenue"), adds a "category" column based on revenue (>50K="high", else "low"), and returns a DataFrame.', starterCode: 'import pandas as pd\n\ndef etl_pipeline(raw_data):\n    # Extract\n    df = pd.DataFrame(raw_data)\n    \n    # Transform\n    \n    \n    # Return cleaned DataFrame\n    return df\n\nraw = [\n    {"name": "A", "revenue": 75000},\n    {"name": "B", "revenue": None},\n    {"name": "C", "revenue": 32000},\n    {"name": "D", "revenue": 91000},\n    {"name": "E", "revenue": 45000}\n]\n\nresult = etl_pipeline(raw)\nprint(result)', tests: [{ name: 'Null removed', code: 'assert len(result) == 4', expectedOutput: null }, { name: 'Category added', code: 'assert "category" in result.columns', expectedOutput: null }] }
], scenarios: [], quiz: [
  { id: 'q1', type: 'multiple_choice', question: 'What does ETL stand for?', options: ['Execute, Test, Launch', 'Extract, Transform, Load', 'Export, Transfer, Link', 'Evaluate, Train, Learn'], correct: 1 },
  { id: 'q2', type: 'scenario', question: 'You need to combine data from a CSV, a JSON API, and a SQL database. Best approach?', options: ['Copy-paste into Excel', 'Read each into a DataFrame, clean, then merge', 'Convert everything to CSV first', 'Use only one source'], correct: 1 },
  { id: 'q3', type: 'multiple_choice', question: 'How to handle API errors in a pipeline?', options: ['Ignore them', 'try/except with logging and retry logic', 'Stop the entire pipeline', 'Use different API'], correct: 1 },
  { id: 'q4', type: 'multiple_choice', question: 'What is idempotent ETL?', options: ['Runs once only', 'Running it multiple times produces the same result', 'Runs in parallel', 'Self-documenting'], correct: 1 },
  { id: 'q5', type: 'multiple_choice', question: 'Best practice for ETL error handling?', options: ['Silent failure', 'Log errors, skip bad records, continue processing', 'Halt on first error', 'Retry indefinitely'], correct: 1 },
  { id: 'q6', type: 'multiple_choice', question: 'How to schedule a Python ETL script?', options: ['while True + sleep', 'cron job (Linux) or Task Scheduler (Windows)', 'Manual execution', 'Using print statements'], correct: 1 }
]};

const room18 = { id: 18, lessons: [
  { title: 'Capstone Project', content: "## Full Analytics Project\n\nIn this capstone, you'll combine everything you've learned to build a complete analytics solution.\n\n### Project Structure\n\n1. **Data Collection**: Gather data from multiple sources\n2. **Data Cleaning**: Handle missing values, outliers, type issues\n3. **Exploratory Analysis**: Statistical summaries and visualizations\n4. **Feature Engineering**: Create meaningful derived metrics\n5. **Analysis/Modeling**: Apply statistical methods or ML\n6. **Visualization**: Create compelling charts and dashboards\n7. **Reporting**: Summarize findings and recommendations\n\n### Best Practices\n\n- Document your code with docstrings\n- Use functions to organize logic\n- Handle errors gracefully\n- Make visualizations clear and labeled\n- Draw actionable conclusions from data" }
], exercises: [
  { id: 'ex1', title: 'Complete Analytics Report', description: 'Using the provided sales data, perform a full analysis: clean the data, calculate KPIs (total revenue, avg order, top product), create a summary by region, and identify the best-performing region. Store the best region name in `best_region`.', starterCode: 'import pandas as pd\nimport numpy as np\n\n# Sales data\ndf = pd.DataFrame({\n    "order_id": range(1, 11),\n    "region": ["North","South","North","East","South","East","North","South","East","North"],\n    "product": ["Widget","Gadget","Widget","Widget","Gadget","Widget","Gadget","Widget","Gadget","Widget"],\n    "revenue": [450, 320, np.nan, 510, 190, 440, 380, 270, 490, 350],\n    "quantity": [5, 3, 4, 6, 2, 5, 4, 3, 5, 4]\n})\n\n# 1. Clean data\n\n\n# 2. Calculate KPIs\ntotal_revenue = \navg_order = \ntop_product = \n\n# 3. Summary by region\nregion_summary = \n\n# 4. Best region\nbest_region = \n\nprint(f"Total Revenue: \${total_revenue:,.2f}")\nprint(f"Avg Order: \${avg_order:,.2f}")\nprint(f"Top Product: {top_product}")\nprint(f"\\nRegion Summary:\\n{region_summary}")\nprint(f"\\nBest Region: {best_region}")', tests: [{ name: 'Data cleaned', code: 'assert df["revenue"].isnull().sum() == 0', expectedOutput: null }, { name: 'Best region found', code: 'assert isinstance(best_region, str) and len(best_region) > 0', expectedOutput: null }] }
], scenarios: [], quiz: [
  { id: 'q1', type: 'scenario', question: 'You\'re asked to analyze why Q3 sales dropped 15%. What\'s your first step?', options: ['Present conclusions immediately', 'Gather data from multiple sources and explore trends', 'Blame the sales team', 'Build a machine learning model'], correct: 1 },
  { id: 'q2', type: 'scenario', question: 'Your analysis reveals an outlier that changes the conclusion. What do you do?', options: ['Remove it without comment', 'Investigate the outlier, document your decision, run analysis with and without it', 'Ignore it', 'Report only favorable results'], correct: 1 },
  { id: 'q3', type: 'multiple_choice', question: 'What makes a good analytics report?', options: ['As many charts as possible', 'Clear findings, actionable recommendations, appropriate visualizations', 'Only raw data tables', 'Complex statistical jargon'], correct: 1 },
  { id: 'q4', type: 'scenario', question: 'A stakeholder questions your methodology. Best response?', options: ['Defend without explanation', 'Walk through your process, show the data, acknowledge limitations', 'Change the results', 'Ignore the feedback'], correct: 1 },
  { id: 'q5', type: 'multiple_choice', question: 'Which Python tool is BEST for creating a reproducible analysis?', options: ['Plain .py scripts', 'Jupyter Notebooks with markdown documentation', 'Excel macros', 'Word documents'], correct: 1 },
  { id: 'q6', type: 'multiple_choice', question: 'When presenting to executives, visualizations should be:', options: ['As detailed as possible', 'Simple, clear, and focused on key metrics', 'Only tables', 'Animated with transitions'], correct: 1 }
]};

export { room14, room15, room16, room17, room18 };
