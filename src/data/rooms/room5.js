const room5 = {
  id: 5,
  lessons: [
    { title: 'Reading Files', content: "## Reading Files\n\nData pipelines start with reading data from files.\n\n### Text Files\n\n```python\n# Using context manager (auto-closes file)\nwith open(\"report.txt\", \"r\") as f:\n    content = f.read()       # Entire file as string\n    # or\n    lines = f.readlines()    # List of lines\n```\n\n### CSV Files (Built-in)\n\n```python\nimport csv\n\nwith open(\"sales.csv\", \"r\") as f:\n    reader = csv.DictReader(f)\n    for row in reader:\n        print(row[\"product\"], row[\"revenue\"])\n```\n\n### JSON Files\n\n```python\nimport json\n\nwith open(\"config.json\", \"r\") as f:\n    config = json.load(f)\n    print(config[\"database\"][\"host\"])\n```\n\n> Context managers (`with` statement) ensure files are properly closed even if an error occurs." },
    { title: 'Writing Files', content: "## Writing Files\n\n```python\n# Write text\nwith open(\"output.txt\", \"w\") as f:\n    f.write(\"Report generated\\n\")\n    f.write(f\"Total: $50,000\\n\")\n\n# Append (don't overwrite)\nwith open(\"log.txt\", \"a\") as f:\n    f.write(\"New entry\\n\")\n```\n\n### Writing CSV\n\n```python\nimport csv\n\ndata = [\n    {\"name\": \"Widget\", \"price\": 29.99, \"qty\": 100},\n    {\"name\": \"Gadget\", \"price\": 49.99, \"qty\": 50}\n]\n\nwith open(\"products.csv\", \"w\", newline=\"\") as f:\n    writer = csv.DictWriter(f, fieldnames=[\"name\", \"price\", \"qty\"])\n    writer.writeheader()\n    writer.writerows(data)\n```\n\n### Writing JSON\n\n```python\nimport json\n\nresults = {\"total\": 150000, \"regions\": [\"North\", \"South\"]}\nwith open(\"results.json\", \"w\") as f:\n    json.dump(results, f, indent=2)\n```" },
    { title: 'Exception Handling', content: "## Exception Handling\n\nRobust data pipelines handle errors gracefully.\n\n```python\ntry:\n    value = int(user_input)\n    result = 100 / value\nexcept ValueError:\n    print(\"Invalid number format\")\nexcept ZeroDivisionError:\n    print(\"Cannot divide by zero\")\nexcept Exception as e:\n    print(f\"Unexpected error: {e}\")\nfinally:\n    print(\"Cleanup complete\")\n```\n\n### Common Data Processing Patterns\n\n```python\ndef safe_float(value, default=0.0):\n    \"\"\"Safely convert to float with fallback.\"\"\"\n    try:\n        return float(value)\n    except (ValueError, TypeError):\n        return default\n\nprices = [\"29.99\", \"N/A\", \"49.99\", \"\", \"19.99\"]\nclean = [safe_float(p) for p in prices]\n# [29.99, 0.0, 49.99, 0.0, 19.99]\n```\n\n### Raising Exceptions\n\n```python\ndef validate_revenue(amount):\n    if not isinstance(amount, (int, float)):\n        raise TypeError(\"Revenue must be a number\")\n    if amount < 0:\n        raise ValueError(\"Revenue cannot be negative\")\n    return amount\n```" }
  ],
  exercises: [
    { id: 'ex1', title: 'CSV Parser', description: 'Parse the CSV string below (simulating file read). Split into rows, then create a list of dictionaries. Each dict should have keys from the header row. Store in `records`.', starterCode: 'csv_data = """name,department,salary\nAlice,Analytics,85000\nBob,Sales,72000\nCarol,Engineering,95000\nDave,Analytics,78000"""\n\n# Parse CSV string into list of dicts\nrecords = []\nlines = csv_data.strip().split("\\n")\n\n\n\nprint(f"Parsed {len(records)} records")\nfor r in records:\n    print(r)', tests: [{ name: 'Record count', code: 'assert len(records) == 4', expectedOutput: null }, { name: 'First record', code: 'assert records[0]["name"] == "Alice" and records[0]["salary"] == "85000"', expectedOutput: null }, { name: 'Has all keys', code: 'assert set(records[0].keys()) == {"name", "department", "salary"}', expectedOutput: null }] },
    { id: 'ex2', title: 'Safe Data Converter', description: 'Write a function `safe_convert(value, target_type, default=None)` that safely converts a value to the target type (int, float, str). Return default if conversion fails.', starterCode: '# Write your function\n\n\n# Test cases\nprint(safe_convert("42", int))         # 42\nprint(safe_convert("3.14", float))     # 3.14\nprint(safe_convert("abc", int, 0))     # 0\nprint(safe_convert(None, float, 0.0))  # 0.0\nprint(safe_convert("", int, -1))       # -1', tests: [{ name: 'Valid int', code: 'assert safe_convert("42", int) == 42', expectedOutput: null }, { name: 'Invalid returns default', code: 'assert safe_convert("abc", int, 0) == 0', expectedOutput: null }, { name: 'None handling', code: 'assert safe_convert(None, float, 0.0) == 0.0', expectedOutput: null }] },
    { id: 'ex3', title: 'JSON Data Transformer', description: 'Given a JSON string of sales data, parse it, calculate total_revenue per region, and create a summary dict. Store in `summary`.', starterCode: 'import json\n\njson_data = \'\'\'{\n    "sales": [\n        {"region": "North", "amount": 45000},\n        {"region": "South", "amount": 32000},\n        {"region": "North", "amount": 28000},\n        {"region": "East", "amount": 51000},\n        {"region": "South", "amount": 19000}\n    ]\n}\'\'\'\n\n# Parse and aggregate by region\ndata = json.loads(json_data)\nsummary = {}\n\n\n\nprint(summary)', tests: [{ name: 'North total', code: 'assert summary["North"] == 73000', expectedOutput: null }, { name: 'South total', code: 'assert summary["South"] == 51000', expectedOutput: null }, { name: 'East total', code: 'assert summary["East"] == 51000', expectedOutput: null }] }
  ],
  scenarios: [
    { id: 'sc1', title: 'Fix the Data Pipeline', description: 'This data processing pipeline has error handling issues. Fix them.', buggyCode: 'import json\n\ndef process_records(raw_data):\n    results = []\n    for record in raw_data:\n        try\n            name = record["name"]\n            value = float(record["value"])\n            results.append({"name": name, "value": value})\n        except KeyError:\n            print(f"Missing field in record")\n        except ValueError:\n            print(f"Invalid value for {record[\'name\']}")\n    return results\n\ntest_data = [\n    {"name": "A", "value": "100"},\n    {"name": "B", "value": "not_a_number"},\n    {"value": "200"},\n    {"name": "D", "value": "400"}\n]\n\nresult = process_records(test_data)\nprint(f"Processed {len(result)} of {len(test_data)} records")', solution: 'import json\n\ndef process_records(raw_data):\n    results = []\n    for record in raw_data:\n        try:\n            name = record["name"]\n            value = float(record["value"])\n            results.append({"name": name, "value": value})\n        except KeyError:\n            print(f"Missing field in record")\n        except ValueError:\n            print(f"Invalid value for {record.get(\'name\', \'unknown\')}")\n    return results\n\ntest_data = [\n    {"name": "A", "value": "100"},\n    {"name": "B", "value": "not_a_number"},\n    {"value": "200"},\n    {"name": "D", "value": "400"}\n]\n\nresult = process_records(test_data)\nprint(f"Processed {len(result)} of {len(test_data)} records")', hints: ['try needs a colon', 'In the ValueError handler, accessing record["name"] could itself raise KeyError'] }
  ],
  quiz: [
    { id: 'q1', type: 'multiple_choice', question: 'What does the `with` statement ensure when working with files?', options: ['Faster reading', 'File is properly closed after use', 'File is encrypted', 'Data is validated'], correct: 1 },
    { id: 'q2', type: 'code_output', question: 'What does this print?\n```python\ntry:\n    x = int("hello")\nexcept ValueError:\n    x = -1\nfinally:\n    x += 1\nprint(x)\n```', options: ['-1', '0', '1', 'Error'], correct: 1 },
    { id: 'q3', type: 'multiple_choice', question: 'Which file mode appends to an existing file?', options: ['"r"', '"w"', '"a"', '"x"'], correct: 2 },
    { id: 'q4', type: 'scenario', question: 'You\'re reading a CSV with 1M rows where some price fields contain "N/A". Best approach?', options: ['Skip the entire file if any error', 'Use try/except inside the loop with a default value', 'Convert all to strings', 'Delete rows with errors before reading'], correct: 1 },
    { id: 'q5', type: 'multiple_choice', question: 'What does `json.loads()` do vs `json.load()`?', options: ['Same thing', 'loads() parses a string, load() reads from a file', 'load() parses a string, loads() reads from a file', 'loads() is deprecated'], correct: 1 },
    { id: 'q6', type: 'code_output', question: 'What happens?\n```python\ntry:\n    result = 10 / 0\nexcept ZeroDivisionError:\n    result = None\nprint(result)\n```', options: ['0', 'Error', 'None', 'Infinity'], correct: 2 },
    { id: 'q7', type: 'multiple_choice', question: 'Which exception catches ALL errors?', options: ['Error', 'BaseException', 'Exception', 'AllErrors'], correct: 2 },
    { id: 'q8', type: 'scenario', question: 'Building an ETL pipeline that reads from 3 different file formats (CSV, JSON, XML). How should errors be handled?', options: ['One giant try/except around everything', 'Separate try/except for each format with specific error messages', 'Ignore errors and process what you can', 'Validate all files before processing any'], correct: 1 }
  ]
};
export default room5;
