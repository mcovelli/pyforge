export const lessons = [
  { title: 'Matplotlib Fundamentals', content: "## Matplotlib: The Visualization Foundation\n\nMatplotlib is Python's foundational plotting library. Every data visualization library in Python is either built on top of it or inspired by it.\n\n### Basic Plot Structure\n\n```python\nimport matplotlib.pyplot as plt\nimport numpy as np\n\n# Simple line plot\nx = [1, 2, 3, 4, 5]\ny = [10, 25, 15, 30, 20]\n\nplt.figure(figsize=(10, 6))\nplt.plot(x, y, color='blue', linewidth=2, marker='o')\nplt.title('Monthly Sales Trend')\nplt.xlabel('Month')\nplt.ylabel('Sales ($K)')\nplt.grid(True, alpha=0.3)\nplt.show()\n```\n\n### The Figure-Axes Architecture\n\n```python\n# More control with subplots\nfig, axes = plt.subplots(1, 2, figsize=(14, 5))\n\n# Left plot\naxes[0].plot(x, y, 'b-o')\naxes[0].set_title('Line Plot')\n\n# Right plot\naxes[1].bar(x, y, color='coral')\naxes[1].set_title('Bar Chart')\n\nplt.tight_layout()\nplt.show()\n```\n\n### Plot Types for Analytics\n\n| Plot Type | Best For | Function |\n|-----------|----------|----------|\n| Line | Trends over time | `plt.plot()` |\n| Bar | Comparing categories | `plt.bar()` |\n| Scatter | Relationship between variables | `plt.scatter()` |\n| Histogram | Distribution of values | `plt.hist()` |\n| Pie | Proportions of a whole | `plt.pie()` |\n| Box | Distribution + outliers | `plt.boxplot()` |" },
  { title: 'Chart Types & Customization', content: "## Common Chart Types\n\n### Bar Charts\n\n```python\nimport matplotlib.pyplot as plt\n\ncategories = ['Q1', 'Q2', 'Q3', 'Q4']\nvalues = [45000, 52000, 48000, 61000]\n\nplt.figure(figsize=(8, 5))\nbars = plt.bar(categories, values, color=['#4e79a7', '#f28e2b', '#e15759', '#76b7b2'])\nplt.title('Quarterly Revenue', fontsize=14, fontweight='bold')\nplt.ylabel('Revenue ($)')\n\n# Add value labels on bars\nfor bar, val in zip(bars, values):\n    plt.text(bar.get_x() + bar.get_width()/2, bar.get_height() + 500,\n             f'${val:,}', ha='center', fontsize=10)\nplt.show()\n```\n\n### Scatter Plots\n\n```python\nimport numpy as np\n\nnp.random.seed(42)\nspend = np.random.uniform(1000, 10000, 50)\nrevenue = spend * np.random.uniform(2, 5, 50)\n\nplt.figure(figsize=(8, 6))\nplt.scatter(spend, revenue, alpha=0.6, c=revenue, cmap='viridis', s=80)\nplt.colorbar(label='Revenue')\nplt.xlabel('Marketing Spend ($)')\nplt.ylabel('Revenue ($)')\nplt.title('Marketing Spend vs Revenue')\nplt.show()\n```\n\n### Histograms\n\n```python\ndata = np.random.normal(75000, 15000, 1000)  # Salary distribution\n\nplt.figure(figsize=(8, 5))\nplt.hist(data, bins=30, color='steelblue', edgecolor='white', alpha=0.8)\nplt.axvline(np.mean(data), color='red', linestyle='--', label=f'Mean: ${np.mean(data):,.0f}')\nplt.axvline(np.median(data), color='green', linestyle='--', label=f'Median: ${np.median(data):,.0f}')\nplt.legend()\nplt.title('Salary Distribution')\nplt.xlabel('Salary ($)')\nplt.ylabel('Frequency')\nplt.show()\n```\n\n### Styling\n\n```python\n# Use a style\nplt.style.use('seaborn-v0_8-darkgrid')\n\n# Custom colors\ncolors = ['#264653', '#2a9d8f', '#e9c46a', '#f4a261', '#e76f51']\n```" },
  { title: 'Multi-Plot Dashboards', content: "## Building Dashboards with Subplots\n\nCombine multiple plots into a single figure for comprehensive data visualization:\n\n```python\nimport matplotlib.pyplot as plt\nimport numpy as np\n\n# Create a 2x2 dashboard\nfig, axes = plt.subplots(2, 2, figsize=(14, 10))\nfig.suptitle('Sales Dashboard Q4 2024', fontsize=16, fontweight='bold')\n\n# Top-left: Line chart (trend)\nmonths = ['Jan','Feb','Mar','Apr','May','Jun']\nsales = [45, 52, 48, 61, 55, 67]\naxes[0,0].plot(months, sales, 'b-o', linewidth=2)\naxes[0,0].set_title('Monthly Trend')\naxes[0,0].set_ylabel('Sales ($K)')\n\n# Top-right: Bar chart (by region)\nregions = ['North', 'South', 'East', 'West']\ntotals = [180, 145, 132, 195]\naxes[0,1].bar(regions, totals, color=['#4e79a7','#f28e2b','#e15759','#76b7b2'])\naxes[0,1].set_title('Sales by Region')\n\n# Bottom-left: Pie chart (market share)\nlabels = ['Product A', 'Product B', 'Product C', 'Other']\nsizes = [35, 28, 22, 15]\naxes[1,0].pie(sizes, labels=labels, autopct='%1.1f%%', startangle=90)\naxes[1,0].set_title('Market Share')\n\n# Bottom-right: Histogram\nnp.random.seed(42)\ndeal_sizes = np.random.exponential(5000, 200)\naxes[1,1].hist(deal_sizes, bins=25, color='steelblue', edgecolor='white')\naxes[1,1].set_title('Deal Size Distribution')\naxes[1,1].set_xlabel('Deal Size ($)')\n\nplt.tight_layout()\nplt.show()\n```\n\n### Saving Plots\n\n```python\nfig.savefig('dashboard.png', dpi=150, bbox_inches='tight')\nfig.savefig('dashboard.pdf')  # Vector format\nfig.savefig('dashboard.svg')  # Web format\n```\n\n### Key Visualization Principles\n\n1. **Choose the right chart type** for your data and message\n2. **Label everything** — title, axes, legends\n3. **Use color intentionally** — highlight, don't distract\n4. **Remove clutter** — gridlines, borders, unnecessary elements\n5. **Tell a story** — guide the viewer's eye to the insight" }
];
